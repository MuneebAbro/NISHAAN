<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:colorBackground"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="@dimen/spacing_xl">

    <!-- Glowing Success Icon Container -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="96dp"
        android:layout_height="96dp"
        app:cardCornerRadius="48dp"
        app:strokeWidth="1.5dp"
        app:strokeColor="@color/color_accent_low"
        app:cardBackgroundColor="@color/color_severity_low_bg"
        app:cardElevation="0dp"
        android:layout_marginBottom="@dimen/spacing_lg">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="✓"
            android:textSize="38sp"
            android:textStyle="bold"
            android:textColor="@color/color_accent_low"
            android:contentDescription="@null" />

    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/confirmation_title"
        android:textSize="24sp"
        style="@style/TextDisplay" />

    <TextView
        android:id="@+id/confirmationBody"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_md"
        android:gravity="center"
        android:textSize="15sp"
        android:textColor="@color/color_on_background_dark"
        style="@style/TextBodyLarge" />

    <!-- Report ID Monospace Info Badge -->
    <com.google.android.material.card.MaterialCardView
        style="@style/NishaanCard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_lg"
        app:cardCornerRadius="8dp"
        app:strokeWidth="1dp"
        app:strokeColor="@color/color_divider_dark"
        app:cardBackgroundColor="@color/color_charcoal_grey">

        <TextView
            android:id="@+id/reportIdText"
            style="@style/TextMono"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="@dimen/spacing_md"
            android:paddingVertical="@dimen/spacing_sm"
            android:textColor="@color/color_on_background_dark"
            android:textSize="13sp" />

    </com.google.android.material.card.MaterialCardView>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnBackToHome"
        style="@style/PrimaryButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_xl"
        android:text="@string/btn_back_to_home" />
</LinearLayout>
